Q) Check if Array is Sorted 

class Solution {
    public boolean check(int[] arr , int n)    {

      for (int i = 1; i < n; i++) {
            if (arr[i] < arr[i - 1])
              return false;
          }
      
          return true;
    }
}

Time Complexity: O(N)

Space Complexity: O(1)
